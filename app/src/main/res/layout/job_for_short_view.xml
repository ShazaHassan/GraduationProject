<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/border_for_job"
    android:layout_margin="10dp"
    android:paddingBottom="10dp"
    android:id="@+id/short_view_to_show_job"
    android:paddingTop="10dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/company_name_shortView"
        android:textSize="28sp"
        android:textColor="@color/white"
        android:text="hi"
        android:layout_margin="10dp" />

    <ImageView
        android:layout_width="match_parent"
        android:id="@+id/logo_of_company_shortView"
        android:adjustViewBounds="true"
        android:layout_margin="10dp"
        android:scaleType="fitXY"
        android:layout_height="200dp"
        app:layout_constraintTop_toBottomOf="@id/company_name_shortView"
        android:maxHeight="200dp"
        android:minHeight="100dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:id="@+id/description_for_position_shortView"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_margin="10dp"
        android:maxLines="2"
        android:minLines="1"
        app:layout_constraintTop_toBottomOf="@id/logo_of_company_shortView"
        android:padding="5dp"
        android:textColor="@color/white" />

    <TextView
        android:id="@+id/job_category_shortView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:padding="5dp"
        android:text="Category"
        android:textColor="@color/white"
        android:textSize="18sp"
        app:layout_constraintTop_toBottomOf="@id/description_for_position_shortView" />


</android.support.constraint.ConstraintLayout>